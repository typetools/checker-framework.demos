language: java

script: travis_wait 120 ./.travis-build.sh

sudo: false
## Reinstate when the build can use a standard jtreg install
# addons:
#   apt:
#     packages:
#       - jtreg

jdk:
  - oraclejdk8
#  - oraclejdk7
  - openjdk7

git:
  depth: 3
